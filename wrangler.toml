# Cloudflare Pages — publish `public/` after `npm run build` (see package.json "build").
# In the Pages project: Build command = npm run build, Build output = public (or rely on this file + dashboard).
name = "records-of-the-grand-historian"
pages_build_output_dir = "public"
