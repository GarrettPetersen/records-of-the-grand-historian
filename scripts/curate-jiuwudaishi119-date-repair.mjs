import path from 'node:path';
import { buildDateAuditPacket, dateAuditDiagnostics, dateAuditItems } from './lib/people-date-audit.mjs';
import { buildPeopleExtractionPacket } from './build-people-extraction-packet.mjs';
import { PEOPLE_DIR, readJson, sha256, writeJsonAtomic, writeTextAtomic } from './lib/people-content.mjs';
import { serializeCompactPeopleExtraction } from './lib/people-compact.mjs';
import { validateCompactPeopleExtraction } from './validate-people-extraction.mjs';
const book='jiuwudaishi', chapter='119', candidateCommit='828130c1393c61415d936a1be3f11ea3ce358040', independentReviewCommit='32029a57e10bbbd244e150b25c59785f6241a73d';
const sealedCandidateHash='sha256:a905395e44ac6b9f01255f769674562798f4836ccdcda9b7d5d461f2f1ccd5f6', expectedCandidateHash='sha256:119d4d917eb8775efeb453bd682b9f5d30cefd0ad1f1227f598d7199f5740ee9';
const candidateFile=`staged-candidate-${sealedCandidateHash.slice(7,27)}.json`, reviewFile=`${sealedCandidateHash.slice(7)}.candidate-review.json`, directory=path.join(PEOPLE_DIR,'date-repairs',book,chapter), handoff=readJson(path.join(directory,candidateFile)), review=readJson(path.join(directory,reviewFile)), extractionPath=path.join(PEOPLE_DIR,'extractions',book,`${chapter}.json`), canonical=readJson(extractionPath), packet=buildDateAuditPacket(book,chapter), same=(a,b)=>JSON.stringify(a)===JSON.stringify(b);
if(packet.sourceHash!==handoff.canonicalSourceHash||packet.extractionHash!==handoff.canonicalExtractionHash||handoff.auditCommit!=='b3ec0acd8ad72056248d122636cf3feef732617d'||sha256(JSON.stringify(readJson(path.join(PEOPLE_DIR,'date-audits',book,`${chapter}.json`))))!==handoff.auditReportHash)throw Error('Canonical or initial-audit pin mismatch');
if(handoff.sealedCandidateHash!==sealedCandidateHash||handoff.candidateExtractionHash!==expectedCandidateHash||sha256(JSON.stringify(handoff.proposal))!==sealedCandidateHash)throw Error('Sealed candidate identity mismatch');
if(review.kind!=='independent-staged-date-repair-review'||review.book!==book||review.chapter!==chapter||review.candidateCommit!==candidateCommit||review.candidateFile!==candidateFile||review.sealedCandidateHash!==sealedCandidateHash||review.candidateHash!==expectedCandidateHash||review.validation?.canonicalSourceHash!==packet.sourceHash||review.validation?.canonicalExtractionHash!==packet.extractionHash||review.disposition!=='accept-for-host-curation'||review.expectedCanonicalAuditStatus!=='requires-fresh-independent-audit')throw Error('Independent review receipt mismatch');
const changes=handoff.proposal.changes, counts=Object.fromEntries(['reclassify-retrospective-reference','hints'].map(kind=>[kind,changes.filter(change=>change.kind===kind).length])); if(changes.length!==28||!same(counts,{'reclassify-retrospective-reference':14,hints:14}))throw Error(`Unexpected operation set: ${JSON.stringify(counts)}`);
const candidate=structuredClone(canonical),seen=new Set();for(const change of changes){if(change.kind==='hints'){const key=`hints-${change.personId}`;if(seen.has(key))throw Error('Duplicate hint repair');seen.add(key);const person=candidate.people.find(row=>row[0]===change.personId);if(!person||!same(person[4]?.a??[],change.before)||!Array.isArray(change.after)||change.after.length)throw Error('Stale or non-empty reception hint repair');person[4]={...person[4],a:change.after};continue}if(change.kind!=='reclassify-retrospective-reference'||!/^claim-[1-9]\d*$/.test(change.id)||seen.has(change.id))throw Error('Invalid claim repair');seen.add(change.id);const index=Number(change.id.slice(6))-1;if(!same(candidate.claims[index],change.before)||change.before[1]!=='attestation'||change.after?.[1]!=='event-participation'||change.after?.[2]?.kind!=='retrospective-reference'||change.after?.[2]?.receptionType!=='retrospective'||change.after?.[2]?.role!=='recalled-subject')throw Error('Stale or invalid retrospective reception repair');candidate.claims[index]=change.after}
if(!same(candidate,handoff.candidate)||sha256(JSON.stringify(candidate))!==expectedCandidateHash)throw Error('Exact replay does not reproduce sealed candidate');const changedTopLevel=Object.keys(canonical).filter(key=>!same(canonical[key],candidate[key]));if(!same(changedTopLevel,['people','claims']))throw Error(`Non-date/top-level drift: ${JSON.stringify(changedTopLevel)}`);const validation=validateCompactPeopleExtraction(candidate,buildPeopleExtractionPacket(book,chapter)),geometry=dateAuditDiagnostics(dateAuditItems(candidate));if(geometry.length)throw Error(`Chronology geometry diagnostics: ${JSON.stringify(geometry)}`);writeTextAtomic(extractionPath,serializeCompactPeopleExtraction(candidate));const curated=readJson(extractionPath);if(!same(curated,candidate)||sha256(JSON.stringify(curated))!==expectedCandidateHash)throw Error('Compact serialization altered candidate');
const receipt={schemaVersion:1,kind:'host-date-repair-curation',book,chapter,candidateCommit,independentReviewCommit,candidateFile,candidateReview:reviewFile,sealedCandidateHash,candidateExtractionHash:expectedCandidateHash,canonicalSourceHash:packet.sourceHash,priorCanonicalExtractionHash:packet.extractionHash,curatedAt:new Date().toISOString(),operations:changes.length,auditGroups:2,scope:{retrospectiveReclassifications:counts['reclassify-retrospective-reference'],hintChanges:counts.hints},validation:{sourceCandidateReviewPins:'PASS',sealedProposalIdentity:'PASS',exactReplay:'PASS',candidateHash:'PASS',scopedDelta:'PASS: 14 retrospective reclassifications and 14 paired hint removals; non-date/top-level drift zero.',compactSchema:`PASS: ${validation.stats.claims} claims.`,chronologyGeometry:'PASS: zero diagnostics.'},publication:'staging-only',masterMutation:'NONE',sharedTreeMutation:'NONE',nextStep:'Fresh independent canonical date audit by a different reviewer before any master publication.'};writeJsonAtomic(path.join(directory,`${sealedCandidateHash.slice(7)}.curation.json`),receipt);console.log(JSON.stringify({candidateHash:expectedCandidateHash,operations:changes.length,counts,changedTopLevel,validation:validation.stats,geometry:geometry.length}));
